# AVL itself is GPL-licensed and distributed separately — never commit the
# binary. See README.md for where to obtain it.
avl352.exe
*.exe

# Geometry is handled deliberately and separately, not defaulted into the repo.
# Anchored to the root so the reference sections/*.dat fixtures still commit.
/*.stl
/*.STL

# Per-run output. Regenerated by pipeline/run_case.py; never worth committing.
outputs/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environments
venv/
.venv/
env/
ENV/

# Editor / OS noise
.DS_Store
Thumbs.db

# NOT ignored, deliberately:
#   avl_iter2/, avl_iter3/  — regression fixtures. tests/test_avl_runner.py and
#                             tests/test_results_fixture_d.py read these real
#                             AVL files directly; the suite is weaker without them.
#   running_log.md          — the accumulated iteration history.
